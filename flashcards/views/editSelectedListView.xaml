<UserControl x:Class="Flashcards.views.editSelectedListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Flashcards.views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView HorizontalAlignment="Left" Height="191" Margin="51,47,0,0" VerticalAlignment="Top" Width="218">

            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>

        </ListView>
        <Button x:Name="addNewPairButton" Content="Add new pair" HorizontalAlignment="Left" Margin="176,10,0,0" VerticalAlignment="Top" Width="93"/>
        <Button x:Name="saveChangesButton" Content="Save" HorizontalAlignment="Left" Margin="194,259,0,0" VerticalAlignment="Top" Width="75" Click="saveChangesButton_Click"/>
        <Button x:Name="cancelChangesButton" Content="Cancel" HorizontalAlignment="Left" Margin="51,259,0,0" VerticalAlignment="Top" Width="75" Click="cancelChangesButton_Click"/>

    </Grid>
</UserControl>
