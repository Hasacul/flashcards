<UserControl x:Class="Flashcards.views.editListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Flashcards.views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <TextBlock x:Name="editListLabel" HorizontalAlignment="Left" Margin="98,52,0,0" TextWrapping="Wrap" Text="Choose list to edit:" VerticalAlignment="Top"/>
        <ListView x:Name="editList" HorizontalAlignment="Left" Height="100" Margin="38,88,0,0" VerticalAlignment="Top" Width="236" SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
