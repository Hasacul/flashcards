<UserControl x:Class="Flashcards.views.loginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Flashcards.views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="28"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="loginBox"  Grid.Column="1" Grid.Row="3" Margin="10,0,10,5" MaxHeight="40" MaxWidth="300"/>
        <Label x:Name="loginLabel"  Content="Login:"  Grid.Column="1" Grid.Row="2" Margin="0,0,0,0" Grid.RowSpan="1" MaxHeight="40" MaxWidth="150" HorizontalContentAlignment="Center"/>
        <Button Content="Sign in"  Click="login_Click" Grid.Column="1" Grid.Row="4" Grid.RowSpan="1" Margin="10,5,10,0" MaxHeight="40" MaxWidth="150"/>

    </Grid>
</UserControl>
